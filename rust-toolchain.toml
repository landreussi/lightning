[toolchain]
# Nightly, because `rustfmt.toml` asks for import grouping/granularity and
# comment wrapping, and rustfmt only honours those on nightly.
channel = "nightly-2026-09-14"
components = ["rustfmt", "clippy"]
